name: Deploy static content to Pages
on:
  push:
    branches: ["main"]
    paths:
      - outboundGenerator.html
      - frontend/src/lib/outboundParser.js
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: "pages"
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Create deployment folder
        run: |
          rm -rf _site
          mkdir -p _site/Outbound_Generator
          cp outboundGenerator.html _site/Outbound_Generator/index.html
          cp frontend/src/lib/outboundParser.js _site/Outbound_Generator/outboundParser.js
          cp generator-favicon.png _site/Outbound_Generator/favicon.png
          echo '<meta http-equiv="refresh" content="0;url=./Outbound_Generator/">' > _site/index.html
      - name: Setup Pages
        uses: actions/configure-pages@v6
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: "./_site"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
